name: "Bump Version"

on:
  pull_request:
    branches:
      - main
      
jobs:
  bump-version:
    name: "Bump Version"
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout source code"
        uses: "actions/checkout@v2"
        with:
          ref: ${{ github.ref }}
#       - name: "Setup Node.js"
#         uses: "actions/setup-node@v1"
#         with:
#           node-version: 12
#       - name: "Automated Version Bump"
#         uses: "phips28/gh-action-bump-version@master"
#         with:
#           tag-prefix: ''
#           minor-wording:  'add,Adds,new'
#           major-wording:  'MAJOR,cut-major'
#           patch-wording:  'patch,fixes'     # Providing patch-wording will override commits

#         env:
#           GITHUB_TOKEN: ${{ secrets.TOKEN_ACTION }}
      - name: Bump version if release label
#         if: github.event.label.name == 'major' || github.event.label.name == 'minor' || github.event.label.name == 'patch'
        run: |
          npm version patch --no-git-tag-version
          git commit -am 'update(package.json):bump ${{ github.event.label.name }} version'
          remote_repo="https://${{ github.actor }}:${{ secrets.TOKEN_ACTION }}@github.com/${{ github.repository }}.git"
          git push "${remote_repo}"

